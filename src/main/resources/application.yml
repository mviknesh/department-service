server:
  port: 9001
spring:  h2:    console:      enabled: true  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:testdb      username: root
    password: root 
      application:    name: DEPARTMET-SERVICE
eureka:  client:    register-with-eureka: true
    fetch-registry: true    service-url:      defaultZone: http://127.0.0.1:8761/eureka/  instance:    hostname: localhost 
 